from pydantic import Field\"""This module contains the Sniffing class, which represents sniffing settings in the XUI API."""\"""Stores the fields returned by the XUI API for parsing."""\"""Constants for Sniffing fields."""\"""Class to store constants related to fields returned by the API."""\"""Class to represent sniffing settings in the XUI API."""\"""Attributes:"""\"""enabled (bool): Whether sniffing is enabled. Required."""\"""dest_override (list[str]): List of destination overrides. Optional."""\"""metadata_only (bool): Whether to only sniff metadata. Optional."""\"""route_only (bool): Whether to only route the sniffed data. Optional."""\"""class SniffingFields:\"""Stores the fields returned by the XUI API for parsing."""\"